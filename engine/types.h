#pragma once

namespace Aquarium::Engine {

    // Universal horizontal direction
    enum class Direction {
        Left = -1,
        Right = 1
    };

    // Universal vertical direction
    enum class VerticalDirection {
        Up = -1,
        Down = 1
    };

} // namespace Aquarium::Engine